import User from './nosql/users';
import Track from './nosql/tracks';
import Storage from './nosql/storage';

const models = {
  usersModel: User,
  tracksModel: Track,
  storageModel: Storage
}

export default models;